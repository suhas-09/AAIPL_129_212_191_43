max_new_tokens: 512
temperature: 0.1 # as answer should be deterministic
top_p: 0.9
repetition_penalty: 1.2
do_sample: false

# remove temperature and top_p when do_sample=false (i.e., for deterministic approach with greedy output)
